name: CI/CD Pipeline

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - main  # or the branch you want to use
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Build with Maven
        run: mvn clean package

  #     - name: Build Docker Image
  #       run: |
  #         docker build . -t your-dockerhub-username/spring-petclinic:latest

  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v2
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}

  #     - name: Push Docker Image
  #       run: |
  #         docker push your-dockerhub-username/spring-petclinic:latest

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v2

  #     - name: Set up kubectl
  #       uses: azure/setup-kubectl@v1
  #       with:
  #         version: 'latest'

  #     - name: Configure kubectl
  #       run: |
  #         echo "${{ secrets.KUBE_CONFIG }}" | base64 --decode > $HOME/.kube/config

  #     - name: Deploy to Kubernetes
  #       run: |
  #         kubectl set image deployment/spring-petclinic spring-petclinic=your-dockerhub-username/spring-petclinic:latest
  #         kubectl rollout status deployment/spring-petclinic
